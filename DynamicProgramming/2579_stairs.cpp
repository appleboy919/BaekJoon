#include <iostream>
using namespace std;
int goUp(int n, int *c, int f) { return 0; }
int main() {
    int n;
    cin >> n;
    int c[n] = {0};
    for (int i = 0; i < n; i++)
        cin >> c[i];

    return 0;
}